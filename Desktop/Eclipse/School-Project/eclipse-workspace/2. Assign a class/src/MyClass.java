/* Begin every class with the first two lines
 * Every Java program has a class name which must match the filename,
 * and that every program must contain the main() method. */

public class MyClass {
	public static void main(String[] args) {
		/* The "{}" marks the beginning and the end of a block code
		 * "System" is a built-in Java class that contains useful members,
		 * such as "out", which is short for output.
		 * The "println()" method, short for print line
		 * Each code statement must end with a semicolon ";"		 */
		
		// We gonna ask then to print the line that we want
		System.out.println("Hello World");
		
		/*But you can also type "print" instead of "prinln" if you want to print in the same line
		 * but don't forget to add an extra space if it's necessary		 */
		
		
	}
}